import { Meta, Story, Canvas, DocsContainer } from '@storybook/addon-docs/blocks';
import theme from '../themes/overview.theme';

<Meta
    title="Docs/Overview"
    parameters={{
        docs: {
          container: ({ children, context }) => (
            <DocsContainer context={context}>
              <div style={{ margin: '-2rem -100px' }}>{children}</div>
            </DocsContainer>
          ),
        }
    }}
/>

<Story id="source-example-overview--basic-sample"/>
